export enum PaymentMethod {
  Cash = 'cash',
  Card = 'card',
}

export enum TransactionMethod {
  Manually = 'manually',
  Reader = 'reader',
  Cash = 'cash',
}

export enum ReaderStatus {
  Online = 'online',
  Offline = 'offline',
}